<%= render partial: "form", locals: { link: @link } %>

<div id="links" class="mt-8 flex flex-col divide-y divid-gray-100">
  <%= render @links %>

  <% if @pagy.pages > 1 %>
  <div class="text-center mt-8 mb-16">
    <%== pagy_nav(@pagy) %>
  </div>
  <% end %>
</div>
